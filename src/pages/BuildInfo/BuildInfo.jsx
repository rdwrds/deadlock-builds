import { useRef } from "react"

const BuildInfo = () => {
    console.log("hellow i am build info")
  return (
    <dialog className="build-info-dialog">BuildInfo</dialog>
  )
}
export default BuildInfo